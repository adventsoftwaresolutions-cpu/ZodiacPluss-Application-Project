class VerificationPendingModel {
  final bool isVerified;
  final String title;
  final String description;

  const VerificationPendingModel({
    required this.isVerified,
    required this.title,
    required this.description,
  });
}